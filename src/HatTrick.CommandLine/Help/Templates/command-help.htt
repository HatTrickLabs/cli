
Usage:
{() => GetExecutableName} {$.Name} {+#each () => GetOps+}{#if !$.MustAssign}[{/if}{$.MostVerboseFlag} {($.GenericType) => GetFriendlyTypeName}{#if !$.MustAssign}]{/if+} {+/each+}

{#if $.Options}
Options:
{#each () => GetOps}
{($) => GetOpDefHelp}
{#if $.HasConstraints}
{#each $.Constraints}
{($) => GetOpDefArgConstraintHelp}
{/each}
{/if}
{/each}
{/if+}
{#if $.HasConstraints}
Command constraints:
{#each $.Constraints}
{($) => GetCommandConstraintHelp}
{/each+}
{/if}
