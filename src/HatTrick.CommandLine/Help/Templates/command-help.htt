
Usage:
{() => GetExecutableName} {$.Name} {+#each $.Options+}{#if !$.MustAssign}[{/if}{$.MostVerboseFlag} {($.GenericType) => GetFriendlyTypeName}{#if !$.MustAssign}]{/if+} {+/each+}

{#if $.Options}
Options:
{#each $.Options}
{($) => GetOpDefHelp}
{#if $.HasConstraints}
{#each $.Constraints}
{($) => GetOpDefArgConstraintHelp}
{/each}
{/if}
{/each}
{/if+}
{#if $.HasConstraints}
Command constraints:
{#each $.Constraints}
{($) => GetCommandConstraintHelp}
{/each+}
{/if}
