
Description:
{()=>indent}{(Help) => GetIndentedBlockedContent}

Usage:
{()=>indent}{() => GetExecutableName} {$.Name} {+#each (false) => GetOptions+}{#if !$.MustAssign}[{/if}{$.Flags.Verbose} {($.GenericType) => GetFriendlyTypeName}{#if !$.MustAssign}]{/if+} {+/each+}

{#if $.HasOptions}
Options:
{#each (false) => GetOptions}
{($) => GetOpDefHelp}
{#if $.HasConstraints}
{#each ($) => GetOpDefConstraints}
{($) => GetOpDefArgConstraintHelp}
{/each}
{/if}
{/each}
{/if+}
{#if $.HasConstraints}
Command constraints:
{#each () => GetCmdDefConstraints}
{($) => GetCommandConstraintHelp}
{/each+}
{/if}
