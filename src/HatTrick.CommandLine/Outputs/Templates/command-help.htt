
Usage:
{() => GetExecutableName} {$.Name} {+#each (false) => GetOptions+}{#if !$.MustAssign}[{/if}{$.Flags.Verbose} {($.GenericType) => GetFriendlyTypeName}{#if !$.MustAssign}]{/if+} {+/each+}

{#if $.HasOptions}
Options:
{#each (false) => GetOptions}
{($) => GetOpDefHelp}
{#if $.HasConstraints}
{#each $.Constraints}
{($) => GetOpDefArgConstraintHelp}
{/each}
{/if}
{/each}
{/if+}
{#if $.HasConstraints}
Command constraints:
{#each $.Constraints}
{($) => GetCommandConstraintHelp}
{/each+}
{/if}
