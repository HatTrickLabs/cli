{?var:blockAt = $.BlockStart}

{$.Target.Name}{($.Target.Help, :blockAt, $.Target.Name.Length, '.') => GetBlockedContent}

{#if $.Commands}
Descendent commands:
{#each $.Commands}
{($) => GetCommandHelp}
{/each}
{/if}

{#if $.Commands}
Run {() => GetExecutableName} -? [command] to see help content for the above listed commands.
{/if+}
